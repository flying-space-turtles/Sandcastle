# Arena topology is configured in the committed config/arena.env file.
#
# This file remains available for future local-only secrets or process-specific
# overrides. Do not duplicate team count, subnet, or topology ports here.
