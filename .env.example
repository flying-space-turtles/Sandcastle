# Optional local environment file.
#
# The generated docker-compose.yml is self-contained: scripts/setup.sh creates
# per-team service build contexts under teams/team<N>/service and Compose builds
# them directly. Keep this file for local overrides as the scaffold grows.
