# Reserved for future arena configuration.
#
# The current generated docker-compose.yml is self-contained. Canonical service
# templates live under services/, and scripts/setup.sh copies them to:
#
#   teams/generated/team<N>/example-vuln
#
# Do not add secrets to a committed environment file. The planned single arena
# configuration source is tracked as SC-002 in:
#
#   docs/PROJECT_AUDIT_AND_BACKLOG.md
